# ============================================
# Configuration ValoMaison
# Copiez ce fichier vers .env et modifiez les valeurs
# ============================================

# === APPLICATION ===
# Cle secrete pour les sessions Flask (generez une cle unique!)
# Commande: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGEZ-MOI-generez-une-cle-secrete-unique

# Environnement: development ou production
FLASK_ENV=production

# === BASE DE DONNEES ===
# Utilisateur PostgreSQL
POSTGRES_USER=valomaison

# Mot de passe PostgreSQL (CHANGEZ-LE!)
POSTGRES_PASSWORD=CHANGEZ-MOI-mot-de-passe-securise

# Nom de la base de donnees
POSTGRES_DB=valomaison

# URL complete (generee automatiquement par docker-compose)
# DATABASE_URL=postgresql://valomaison:password@db:5432/valomaison

# === GUNICORN ===
# Nombre de workers (recommande: 2 * CPU + 1)
GUNICORN_WORKERS=4

# Adresse d'ecoute
GUNICORN_BIND=0.0.0.0:5000

# Niveau de log: debug, info, warning, error
GUNICORN_LOG_LEVEL=info

# === DOMAINE & SSL ===
DOMAIN=valomaison.fr
SSL_EMAIL=admin@valomaison.fr
